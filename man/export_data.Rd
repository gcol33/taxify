% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export-data.R
\name{export_data}
\alias{export_data}
\title{Export a taxify result to file}
\usage{
export_data(x, path, overwrite = FALSE)
}
\arguments{
\item{x}{A data.frame returned by \code{\link[=taxify]{taxify()}}.}

\item{path}{Character. Output file path. The format is inferred from the
extension: \code{.vtr}, \code{.csv}, \code{.tsv}, or \code{.xlsx}.}

\item{overwrite}{Logical. Overwrite an existing file? Default \code{FALSE}.}
}
\value{
Invisibly returns \code{path}.
}
\description{
Writes a \code{\link[=taxify]{taxify()}} result (with any enrichments) to disk in one of several
formats. The default \code{.vtr} format preserves column types and is fast to
re-read with \code{\link[=add_data]{add_data()}}.
}
\examples{
# Runs offline against the bundled example database.
old <- options(taxify.data_dir = taxify_example_data())

result <- taxify(c("Quercus robur", "Pinus sylvestris"))
result |> export_data(tempfile(fileext = ".vtr"))
result |> export_data(tempfile(fileext = ".csv"))
result |> export_data(tempfile(fileext = ".tsv"))

options(old)

}
