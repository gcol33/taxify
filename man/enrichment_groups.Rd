% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrichment.R
\name{enrichment_groups}
\alias{enrichment_groups}
\title{Browse the group values a grouped enrichment can filter on}
\usage{
enrichment_groups(source, verbose = TRUE)
}
\arguments{
\item{source}{Character. A grouped enrichment name (see \code{\link[=list_enrichments]{list_enrichments()}}).}

\item{verbose}{Logical. Print the group column and count. Default \code{TRUE}.}
}
\value{
A character vector of the available group values, sorted. Stops with a
pointer to \code{\link[=enrichment_cols]{enrichment_cols()}} when \code{source} is a flat (non-grouped)
enrichment, which has no group values.
}
\description{
Some enrichment doors attach data per group: GRIIS invasive status by country
(\code{\link[=add_griis]{add_griis()}}), WCVP native ranges by TDWG region (\code{\link[=add_wcvp]{add_wcvp()}}), vernacular
names by language (\code{\link[=add_common_names]{add_common_names()}}), alien first records by country
(\code{\link[=add_alien_first_records]{add_alien_first_records()}}). This lists the valid group values for such a
door, the way \code{\link[=enrichment_cols]{enrichment_cols()}} lists a door's columns, so a country,
region, or language code need not be guessed. Read offline from the local
\code{.vtr} metadata (falling back to the manifest, then a scan of the \code{.vtr}); the
first call may trigger the one-time download.
}
\examples{
\donttest{
old <- options(taxify.data_dir = taxify_example_data())
enrichment_groups("griis")   # ISO country codes GRIIS covers
options(old)
}
}
\seealso{
\code{\link[=enrichment_cols]{enrichment_cols()}}, \code{\link[=list_enrichments]{list_enrichments()}}, \code{\link[=add_griis]{add_griis()}},
\code{\link[=add_wcvp]{add_wcvp()}}, \code{\link[=add_common_names]{add_common_names()}}, \code{\link[=add_alien_first_records]{add_alien_first_records()}}
}
