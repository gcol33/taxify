% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add-alien-first-records.R
\name{add_alien_first_records}
\alias{add_alien_first_records}
\title{Add alien species first record years}
\usage{
add_alien_first_records(x, country, verbose = TRUE)
}
\arguments{
\item{x}{A data.frame returned by \code{\link[=taxify]{taxify()}}.}

\item{country}{Character. ISO 3166-1 alpha-2 country code(s), or \code{"all"}.
\itemize{
\item Single code (e.g., \code{"AT"}): adds columns without suffix.
\item Multiple codes (e.g., \code{c("AT", "DE")}): adds columns with
country suffix (e.g., \code{alien_first_record_AT}).
\item \code{"all"}: adds one column set per country in the dataset.
}}

\item{verbose}{Logical. Default \code{TRUE}.}
}
\value{
The same data.frame with additional column(s):
\describe{
\item{alien_first_record}{Year of the first record (integer), or \code{NA}
if not recorded for that country.}
\item{alien_first_record_source}{Database that contributed the record
(e.g., \code{"GAVIA"}, \code{"CABI ISC"}).}
\item{alien_first_record_reference}{Original citation or reference for
the record.}
}
}
\description{
Joins alien species first record data to a \code{\link[=taxify]{taxify()}} result, filtered
by country. Data from the Global Alien Species First Record Database
(Seebens et al. 2017).
}
\details{
Source: Global Alien Species First Record Database v3.1
(Seebens et al. 2017, Nature Communications 8, 14435). CC BY 4.0.
Coverage: ~77k species x country combinations across all taxa.
}
\examples{
\dontrun{
taxify("Robinia pseudoacacia") |>
  add_alien_first_records(country = "AT")

taxify(c("Robinia pseudoacacia", "Ailanthus altissima")) |>
  add_alien_first_records(country = c("AT", "DE"))
}

}
