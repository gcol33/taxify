% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add-alien-first-records.R
\name{add_alien_first_records}
\alias{add_alien_first_records}
\title{Add alien species first record years}
\usage{
add_alien_first_records(x, location, cols = NULL, verbose = TRUE)
}
\arguments{
\item{x}{A data.frame returned by \code{\link[=taxify]{taxify()}}.}

\item{location}{Character. Location key(s), or \code{"all"}. A location that is a
whole country is given by its ISO 3166-1 alpha-2 code (e.g. \code{"AT"}, \code{"US"});
an island or other part of a country that the source records as a region
of its own is given by its name (e.g. \code{"Hawaii"}, \code{"Canary Islands"}).
\itemize{
\item Single location (e.g., \code{"AT"}): adds columns without suffix.
\item Multiple locations (e.g., \code{c("AT", "DE")}): adds columns with
a location suffix (e.g., \code{alien_first_record_AT}).
\item \code{"all"}: adds one column set per location in the dataset.
}
List the locations covered with
\code{enrichment_groups("alien_first_records")}.}

\item{cols}{Character vector of value columns to attach (from
\code{alien_first_record}, \code{alien_first_record_source},
\code{alien_first_record_reference}, plus the dataset's further columns such as
\code{country_code} and \code{alien_first_record_status}), or \code{"all"}. \code{NULL}
(default) attaches the first three.}

\item{verbose}{Logical. Default \code{TRUE}.}
}
\value{
The same data.frame with additional column(s):
\describe{
\item{alien_first_record}{Year of the first record (integer), or \code{NA}
if not recorded for that location.}
\item{alien_first_record_source}{Database that contributed the record
(e.g., \code{"GAVIA"}, \code{"CABI ISC"}).}
\item{alien_first_record_reference}{Original citation or reference for
the record.}
}
}
\description{
Joins alien species first record data to a \code{\link[=taxify]{taxify()}} result, filtered
by location. Data from the FirstRecords dataset (Seebens & Renard Truong
2026; Seebens et al. 2017).
}
\details{
The source's locations do not overlap: a country's location excludes the
islands and other parts recorded separately, so \code{location = "US"} is the
first record for the United States without Hawaii and Alaska, and a species
first recorded on Hawaii has no \code{"US"} record unless the source holds one for
the country itself. Seebens et al. (2017) define the set as "282
non-overlapping regions (countries and sub-national regions such as
islands)". Every row carries \code{country_code}, the ISO 3166-1 alpha-2 code of
the country the location lies in, so a country-wide first record is an
explicit roll-up: request the country's locations and take the earliest year.

Source: FirstRecords v4.0 (Seebens & Renard Truong 2026,
doi:10.5281/zenodo.18759840), CC BY 4.0, one row per species x location.
}
\examples{
# Runs offline against the bundled example database.
old <- options(taxify.data_dir = taxify_example_data())

taxify("Robinia pseudoacacia") |>
  add_alien_first_records(location = "AT")

taxify(c("Robinia pseudoacacia", "Ailanthus altissima")) |>
  add_alien_first_records(location = c("AT", "DE", "Hawaii"))

options(old)

}
\references{
Seebens H, Blackburn TM, Dyer EE, et al. (2017) No saturation in the
accumulation of alien species worldwide. Nature Communications 8:14435.
}
