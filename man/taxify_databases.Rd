% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backbones.R
\name{taxify_databases}
\alias{taxify_databases}
\title{One overview of every database taxify knows about}
\usage{
taxify_databases(verbose = TRUE)
}
\arguments{
\item{verbose}{Logical. When \code{TRUE} (default), prints a one-line count of
backbones, enrichments, and registered traits.}
}
\value{
A data.frame with columns: \code{type} (\code{"backbone"} or \code{"enrichment"}),
\code{name}, \code{scope} (taxonomic scope for backbones; provided trait columns for
enrichments), \code{n_rows}, \code{version}, \code{installed}, \code{source}.
}
\description{
Stacks the taxonomic backbones (from \code{\link[=list_backbones]{list_backbones()}}) and the trait/status
enrichments (from \code{\link[=list_enrichments]{list_enrichments()}}) into a single frame with a \code{type}
column, so a new user can see the full breadth in one call rather than needing
to know three separate discovery verbs. The cross-source trait vocabulary is
summarised in the message footer; browse it with \code{\link[=list_traits]{list_traits()}}.
}
\examples{
\donttest{
taxify_databases()
}

}
\seealso{
\code{\link[=list_backbones]{list_backbones()}}, \code{\link[=list_enrichments]{list_enrichments()}}, \code{\link[=list_traits]{list_traits()}}.
}
