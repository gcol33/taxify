% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add-euromed-distribution.R
\name{add_euromed_distribution}
\alias{add_euromed_distribution}
\title{Add Euro+Med PlantBase distribution status}
\usage{
add_euromed_distribution(x, region, cols = NULL, verbose = TRUE)
}
\arguments{
\item{x}{A data.frame returned by \code{\link[=taxify]{taxify()}}.}

\item{region}{Character. ISO 3166-1 alpha-2 country code(s), Euro+Med area
code(s), or \code{"all"}. A code that is an ISO code of a Euro+Med area is read
as the country; anything else must be an area code, as listed by
\code{\link[=euromed_areas]{euromed_areas()}}.
\itemize{
\item Single region: adds \code{euromed_status} (no suffix).
\item Several regions: adds \verb{euromed_status_<code>} per region.
\item \code{"all"}: one column per area.
}}

\item{cols}{Which columns to attach. \code{NULL} (the default) attaches
\code{euromed_status}; a character vector attaches just those, and \code{"all"}
attaches every column the source carries (see \code{\link[=enrichment_cols]{enrichment_cols()}}), among
them \code{euromed_status_detail}, \code{euromed_status_source}, \code{area_name},
\code{iso2} and the taxon's Euro+Med UUID, \code{taxon_id}.}

\item{verbose}{Logical. Default \code{TRUE}.}
}
\value{
The same data.frame with \code{euromed_status}, one of \code{"native"},
\code{"naturalised"}, \code{"introduced"}, \code{"casual"}, \code{"cultivated"},
\code{"doubtful"} or \code{"extinct"}, and \code{NA} where Euro+Med states nothing for the
region. \code{euromed_status_detail} carries the term as Euro+Med words it
(\code{"endemic"}, \code{"introduced: uncertain degree of naturalisation"}).
\code{euromed_status_source} holds the ids of the citing references; resolve
them with \code{cite(x$euromed_status_source, source = "euromed_distribution")}.
}
\description{
Joins the per-area status Euro+Med PlantBase states for a taxon (native,
naturalised, introduced, casual, ...) to a \code{\link[=taxify]{taxify()}} result, for the areas
or countries asked for. The names and the ids of the \code{euromed} backbone are
the same taxa the status is recorded on, so a result matched against any
backbone joins through its accepted name.
}
\details{
Source: Euro+Med PlantBase (CC BY-SA), European and Mediterranean vascular
plants, bryophytes and some fungi and algae. A report Euro+Med marks as made
in error is not carried; a former presence is \code{"extinct"}. Where the records
of one taxon and area disagree, the stronger claim wins in the order given
above (a taxon is native wherever a record says so), and the references are
those of the records stating it. The country of an area comes from its own
name, so a combined area has none and its countries are read from the
level-2 areas.
}
\examples{
# Runs offline against the bundled example database.
old <- options(taxify.data_dir = taxify_example_data())

taxify("Solidago canadensis") |>
  add_euromed_distribution(region = "DE")

options(old)

}
\seealso{
\code{\link[=euromed_areas]{euromed_areas()}}, \code{\link[=add_wcvp]{add_wcvp()}}
}
