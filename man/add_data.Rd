% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add-data.R
\name{add_data}
\alias{add_data}
\title{Add custom data by taxonomic matching}
\usage{
add_data(
  x,
  data,
  species_col = NULL,
  table = NULL,
  cols = NULL,
  fuzzy = TRUE,
  fuzzy_threshold = 0.2,
  verbose = TRUE
)
}
\arguments{
\item{x}{A data.frame returned by \code{\link[=taxify]{taxify()}}.}

\item{data}{One of:
\itemize{
\item A \strong{data.frame} already in R.
\item A \strong{file path} to a \code{.csv}, \code{.csv.gz}, \code{.xlsx}, \code{.sqlite}/\code{.db}, or
\code{.vtr} file (read via vectra).
}}

\item{species_col}{Character. Name of the column in \code{data} that contains
species names. If \code{NULL} (default), auto-detected by matching \code{head(10)}
of each character column against the backbone.}

\item{table}{Character. Required when \code{data} is a SQLite file --- the table
name to read.}

\item{cols}{Character vector of column names from \code{data} to join. If \code{NULL}
(default), all columns except \code{species_col} are joined.}

\item{fuzzy}{Logical. Enable fuzzy matching for names in \code{data}.
Default \code{TRUE}.}

\item{fuzzy_threshold}{Numeric. Maximum allowed distance for fuzzy matches.
Default \code{0.2}.}

\item{verbose}{Logical. Default \code{TRUE}.}
}
\value{
The input data.frame with additional columns from \code{data}, joined
via backbone-resolved \code{accepted_id}. Columns from \code{data} that collide
with existing columns in \code{x} are prefixed with \code{"data_"}.
}
\description{
Joins an external data source (CSV file or data.frame) to a \code{\link[=taxify]{taxify()}}
result. Species names in the external data are matched through the same
backbone(s) used in the original \code{taxify()} call, and the join is performed
on \code{accepted_id} --- so synonyms in either dataset resolve to the same key.
}
\details{
The workflow:
\enumerate{
\item Read \code{data} (CSV or data.frame).
\item Identify the species column (explicit or auto-detected).
\item Match species names through the same backbone(s) as the original
\code{taxify()} call, obtaining \code{accepted_id} for each row.
\item Check for conflicting duplicates: if multiple rows in \code{data} resolve
to the same \code{accepted_id} with different values, an error is raised.
Exact duplicates produce a warning and are deduplicated.
\item Left-join on \code{accepted_id}.
}
\subsection{Auto-detection}{

When \code{species_col} is not specified, \code{add_data()} takes the first 10 rows
of each character column and runs them through \code{taxify()}. The column with
the highest match rate is selected. If no column achieves at least 50\%
matches, an error is raised asking the user to specify \code{species_col}
explicitly.
}
}
\examples{
\dontrun{
result <- taxify(c("Quercus robur", "Pinus sylvestris"))

# From a CSV file (auto-detect species column)
result |> add_data("my_traits.csv")

# From a SQLite database
result |> add_data("traits.sqlite", table = "plant_traits")

# From a data.frame with explicit species column
traits <- data.frame(species = c("Quercus robur", "Pinus sylvestris"),
                      height = c(30, 25))
result |> add_data(traits, species_col = "species")

# Select specific columns
result |> add_data(traits, species_col = "species", cols = "height")
}

}
