% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classification-tree.R
\name{lowest_common}
\alias{lowest_common}
\title{Lowest common taxon of a set of names}
\usage{
lowest_common(x, backbone = NULL, verbose = TRUE)
}
\arguments{
\item{x}{Character vector of names (two or more), or a \code{\link[=taxify]{taxify()}} result.}

\item{backbone}{Backend passed to \code{\link[=taxify]{taxify()}} when \code{x} is raw names. \code{NULL}
(default) uses every installed backbone. Ignored when \code{x} is a result.}

\item{verbose}{Logical. Default \code{TRUE}.}
}
\value{
A one-row data.frame with columns \code{rank}, \code{name} (the shared taxon),
and \code{n_taxa} (how many resolved names went into the comparison). \code{rank} and
\code{name} are \code{NA} when the taxa share nothing (e.g. different kingdoms, or
only one resolved).
}
\description{
Resolves the names and reports the deepest Linnaean rank at which they all
share one classification value -- their most recent common ancestor in the
backbone's hierarchy. Two congeners return their shared genus; two plants in
different families return a shared order or class; unrelated taxa share only a
kingdom (or nothing).
}
\examples{
# Runs offline against the bundled example database.
old <- options(taxify.data_dir = taxify_example_data())

lowest_common(c("Quercus robur", "Quercus petraea"))

options(old)

}
\seealso{
\code{\link[=class2tree]{class2tree()}}, \code{\link[=add_classification]{add_classification()}}, \code{\link[=taxify]{taxify()}}.
}
