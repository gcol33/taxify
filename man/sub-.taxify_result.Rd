% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/taxify-result.R
\name{[.taxify_result}
\alias{[.taxify_result}
\title{Subset a taxify_result, preserving its metadata}
\usage{
\method{[}{taxify_result}(x, ...)
}
\arguments{
\item{x}{A \code{taxify_result} object.}

\item{...}{Row/column indices passed to the data.frame \code{[} method.}
}
\value{
The subset: a \code{taxify_result} with \code{taxify_meta} preserved while it
remains a data.frame, otherwise the bare column.
}
\description{
The default data.frame \code{[} method drops the \code{taxify_meta} attribute that the
downstream doors (\code{\link[=add_data]{add_data()}}, \code{\link[=cite]{cite()}}, \code{\link[=summary]{summary()}}, \code{\link[=taxify_lock]{taxify_lock()}}) read.
This method carries \code{taxify_meta} and the \code{taxify_result} class through
row/column subsetting, so a subset (including one taken internally by a door
that reorders columns) still exposes its provenance. A subset that collapses
to a single column via \code{drop = TRUE} returns the bare vector, as it would for
a plain data.frame.
}
\keyword{internal}
