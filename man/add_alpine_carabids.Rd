% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add-alpine-carabids.R
\name{add_alpine_carabids}
\alias{add_alpine_carabids}
\title{Add Alpine ground-beetle traits (Chamberlain et al.)}
\usage{
add_alpine_carabids(x, cols = NULL, verbose = TRUE)
}
\arguments{
\item{x}{A data.frame returned by \code{\link[=taxify]{taxify()}}.}

\item{cols}{Which columns to attach: \code{NULL} (default) both, or a
character vector of names. See \code{\link{enrichment_cols}}.}

\item{verbose}{Logical. Default \code{TRUE}.}
}
\value{
The same data.frame with \code{alpine_} columns: numeric
\code{body_length_mm}, categorical \code{wing_morph}.
}
\description{
Joins body size and wing morphology for the carabid fauna of the Italian
Alps to a \code{\link[=taxify]{taxify()}} result by accepted name.
}
\details{
Source: Chamberlain et al. (2020), 185 species recorded at 416 sites
between 697 and 2840 m in the Italian Alps. CC0.

The widest European carabid trait table after \code{\link[=add_chowdhury]{add_chowdhury()}}, and the
one that reaches the Alpine fauna the lowland compilations miss. Like every
European carabid source its body sizes trace back to carabids.org, so
agreement with those is not corroboration; it runs 1.03 against them, the
same quantity without being the verbatim copy \code{\link[=add_eberswalde]{add_eberswalde()}} is.

\code{wing_morph} arrives as bare letters with no legend in the file. The codes
were read off the data rather than assumed: crossed against
\code{\link[=add_chowdhury]{add_chowdhury()}}'s words, \code{b} is short-winged, \code{m} long-winged and \code{d}
dimorphic.
}
\examples{
\dontrun{
taxify(c("Abax exaratus", "Carabus depressus")) |>
  add_alpine_carabids()
}

}
\references{
Chamberlain D, Gobbi M, Negro M, et al. (2020) Trait-modulated decline of
carabid beetle occurrence along elevation gradients across the European Alps.
Journal of Biogeography 47:1030-1041. \doi{10.1111/jbi.13792}

Data: \doi{10.5061/dryad.fn2z34tq1}
}
