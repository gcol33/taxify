% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add-virion.R
\name{add_virion}
\alias{add_virion}
\title{Add host-virus association breadth (VIRION)}
\usage{
add_virion(x, cols = NULL, verbose = TRUE)
}
\arguments{
\item{x}{A data.frame returned by \code{\link[=taxify]{taxify()}}.}

\item{cols}{Which columns to attach: \code{NULL} (default) all, or a
character vector of names. See \code{\link{enrichment_cols}}.}

\item{verbose}{Logical. Default \code{TRUE}.}
}
\value{
The same data.frame with \code{virion_} columns: \code{virus_richness},
\code{virus_family_count}, \code{virus_record_count}, \code{host_class}.
}
\description{
Joins per-host counts of recorded virus associations to a \code{\link[=taxify]{taxify()}} result.
}
\details{
Source: Carlson et al. (2022), 4223 vertebrate hosts and 9833
viruses. ODbL-1.0. As with \code{\link[=add_globi]{add_globi()}} and \code{\link[=add_invacost]{add_invacost()}}, only the
derived per-host counts are redistributed, not the association records.

These counts measure how much a host has been looked at as much as what
infects it. \emph{Homo sapiens} leads with 936 distinct viruses across 633,053
records, and the ordering below it tracks livestock and laboratory species.
\code{virus_record_count} travels alongside so the effort behind a richness is
visible; treat richness as association breadth as recorded, not as a
biological property of the host.
}
\examples{
\dontrun{
taxify(c("Sus scrofa", "Myotis lucifugus")) |>
  add_virion()
}

}
\references{
Carlson CJ, Gibb RJ, Albery GF, et al. (2022) The Global Virome in One
Network (VIRION): an atlas of vertebrate-virus associations. mBio
13:e0298521. \doi{10.1128/mbio.02985-21}
}
