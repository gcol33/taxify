% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add-gidias.R
\name{add_gidias}
\alias{add_gidias}
\title{Add invasive-species impact (EICAT / SEICAT, GIDIAS)}
\usage{
add_gidias(x, group = "Any", cols = NULL, verbose = TRUE)
}
\arguments{
\item{x}{A data.frame returned by \code{\link[=taxify]{taxify()}}.}

\item{group}{Character. Which affected native taxon the impact is measured
against:
\itemize{
\item \code{"Any"} (default): every impact record for the species, the
species-level summary.
\item One of \code{"Plant"}, \code{"Invertebrate"}, \code{"Vertebrate"}, \code{"Microbe"},
\code{"Fungi"}: only the impacts recorded against that group.
\item Several (e.g. \code{c("Plant", "Vertebrate")}): adds columns with a
group suffix (e.g. \code{gidias_eicat_category_Plant}).
\item \code{"all"}: one column set per group, \code{"Any"} included.
}
List them with \code{enrichment_groups("gidias")}.}

\item{cols}{Which columns to attach: \code{NULL} (default) the curated set,
\code{"all"} every column the source carries, or a character vector of
names. See \code{\link{enrichment_cols}}.}

\item{verbose}{Logical. Default \code{TRUE}.}
}
\value{
The same data.frame with additional columns. The default set:
\describe{
\item{gidias_eicat_category}{EICAT environmental-impact category
(\code{MC}/\code{MN}/\code{MO}/\code{MR}/\code{MV}/\code{DD}, or \code{NA} when no negative impact is
recorded).}
\item{gidias_eicat_mechanism}{IUCN EICAT impact mechanism(s) behind the
species' negative environmental impacts.}
\item{gidias_seicat_category}{SEICAT socio-economic-impact category
(\code{MC}/\code{MN}/\code{MO}/\code{MR}/\code{DD}, or \code{NA}).}
\item{gidias_ncp_direction}{Direction of the species' impact on nature's
contributions to people: \code{"Negative"}, \code{"Positive"}, \code{"Neutral"}, or a
combination (\code{"Negative; Positive"}) where the records disagree. GIDIAS
scores no magnitude for this block, so there is no category to report.}
\item{gidias_ias_taxon}{Functional group: \code{Plant}, \code{Invertebrate},
\code{Vertebrate}, or \code{Microbe}.}
\item{gidias_realms}{Realm(s) the impacts span: terrestrial, freshwater,
and/or marine.}
\item{gidias_n_records}{Number of impact records for the species.}
\item{gidias_n_sources}{Number of distinct sources documenting the impacts.}
}
\code{cols = "all"} additionally attaches the numeric EICAT/SEICAT magnitudes
(0-3), the affected well-being constituents and contributions to people
(\code{gidias_ncp_affected}), kingdom, the negative-record count, and a
global-extinction flag.
}
\description{
Joins per-species environmental- and socio-economic-impact aggregates from
GIDIAS (Bacher et al. 2025), the IPBES invasive-species assessment's global
impact compilation, to a \code{\link[=taxify]{taxify()}} result by looking up \code{accepted_name}.
GIDIAS's individual impact records are reduced to per-species indicators; the
raw impact records are not distributed.
}
\details{
A species' EICAT category is the most severe magnitude among its negative
(harmful) environmental-impact records, on the IUCN scale \code{"MC"} (Minimal
Concern), \code{"MN"} (Minor), \code{"MO"} (Moderate), \code{"MR"} (Major, local
extinction), \code{"MV"} (Massive, global extinction), or \code{"DD"} (Data Deficient);
\code{gidias_seicat_category} applies the same rule to socio-economic impact
(SEICAT, \code{"MC"} to \code{"MR"}). For impact reconciled across sources, use
\code{\link[=add_trait]{add_trait()}} with \code{"environmental_impact"} or \code{"socioeconomic_impact"}.

Source: GIDIAS (Bacher et al. 2025, Scientific Data; CC BY 4.0), compiled for
the IPBES thematic assessment report on invasive alien species. Only the
derived per-species aggregates are distributed here, not the raw impact
records.
}
\section{What a species impacts}{

GIDIAS records the affected native taxon per impact record, so \code{group}
asks the question at that grain: \code{add_gidias(group = "Invertebrate")} scores
\emph{Felis catus} \code{"MO"} (reduced populations) where the default \code{"Any"} scores
it \code{"MV"}, the global extinctions it drove among vertebrates. 28\% of species
with a recorded affected taxon impact two or more of the five groups, so for
those the species-level category is the most severe impact on anything, not
the impact on each thing.

The vocabulary is the coarse one GIDIAS controls: \code{"Vertebrate"}, not
\code{"Aves"}. \code{"Any"} is the only group carrying the impact records with no
affected taxon recorded (12\% of the negative ones) and the two people-facing
blocks, so \code{gidias_seicat_category} and \code{gidias_ncp_direction} are \code{NA} on
every other group: impact on people is not a question the
affected-native-taxon axis can answer.
}

\examples{
\dontrun{
# Downloads the GIDIAS enrichment on first use.
taxify("Felis catus", backbone = "gbif") |>
  add_gidias()

# What the cat does to invertebrates, rather than to anything at all.
taxify("Felis catus", backbone = "gbif") |>
  add_gidias(group = "Invertebrate")

taxify("Felis catus", backbone = "gbif") |>
  add_gidias(group = c("Invertebrate", "Vertebrate"))
}

}
\references{
Bacher S et al. (2025) Global Impacts Dataset of Invasive Alien Species
(GIDIAS). Scientific Data 12:832. \doi{10.1038/s41597-025-05184-5}
}
