% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add-combine.R
\name{add_combine_imputed}
\alias{add_combine_imputed}
\title{Add mammal traits from COMBINE (phylogenetically imputed values)}
\usage{
add_combine_imputed(x, cols = NULL, verbose = TRUE)
}
\arguments{
\item{x}{A data.frame returned by \code{\link[=taxify]{taxify()}}.}

\item{cols}{Which columns to attach: \code{NULL} (default) the curated set,
\code{"all"} every column the source carries, or a character vector of
names. See \code{\link{enrichment_cols}}.}

\item{verbose}{Logical. Default \code{TRUE}.}
}
\value{
The same data.frame with additional columns, mirroring
\code{\link[=add_combine_reported]{add_combine_reported()}} but named \verb{combine_imputed_*} (e.g.
\code{combine_imputed_adult_mass_g}, \code{combine_imputed_gestation_length_d}).
Traits COMBINE does not impute (for example \code{biogeographical_realm} and
\code{habitat_breadth_n}) carry the reported coverage; the life-history traits
(gestation, weaning, longevity, litter size, generation length, ...) are
near-complete.
}
\description{
Joins the phylogenetically imputed COMBINE table to a \code{\link[=taxify]{taxify()}} result by
looking up \code{accepted_name}. This is the companion to \code{\link[=add_combine_reported]{add_combine_reported()}}:
the same ~6.2k mammal species, but with the cells the reported table leaves
missing filled in by COMBINE's phylogenetic multiple-imputation model, giving
near-complete coverage for the life-history traits. Imputed values are model
estimates, not measurements, so they are attached under their own
\verb{combine_imputed_*} columns rather than replacing the reported values.
}
\details{
Source: COMBINE (Soria et al. 2021, Ecology, CC0), imputed table
(\code{trait_data_imputed.csv}). Same species set and column meanings as
\code{\link[=add_combine_reported]{add_combine_reported()}}; the difference is coverage. Because imputation only
fills gaps, applying both doors and comparing \verb{combine_*} with
\verb{combine_imputed_*} shows which values are measured versus estimated.
}
\examples{
\dontrun{
# Compare reported vs imputed gestation length for a mammal:
taxify("Vulpes vulpes", backbone = "gbif") |>
  add_combine_reported() |>
  add_combine_imputed()
}

}
\references{
Soria CD et al. (2021) COMBINE: a coalesced mammal database of intrinsic and
extrinsic traits. Ecology 102:e03344. \doi{10.1002/ecy.3344}
}
\seealso{
\code{\link[=add_combine_reported]{add_combine_reported()}} for the measured values; \code{\link[=add_combine]{add_combine()}}
for the two coalesced with per-trait provenance.
}
