% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean.R
\name{normalize_aggregate_name}
\alias{normalize_aggregate_name}
\title{Normalize aggregate markers on canonical names (build-time)}
\usage{
normalize_aggregate_name(name, rank = NULL)
}
\arguments{
\item{name}{Character vector of canonical names.}

\item{rank}{Optional character vector of taxon ranks, the same length as
\code{name}. When supplied, aggregate-rank rows without a marker are suffixed.}
}
\value{
\code{name} with aggregate markers normalized to \code{" aggr."}.
}
\description{
Folds every aggregate marker a backbone or enrichment source may use to one
canonical form, \code{"<binomial> aggr."}, so taxify's matching engine and
enrichment join recognize aggregates uniformly regardless of source spelling.
Two cases are handled:
\itemize{
\item a name already carrying a marker (\code{agg.}, \code{aggr.}, \code{-agg}, \code{s.l.},
\verb{sensu lato}, \verb{coll. sp.}) is rewritten to \code{"<binomial> aggr."};
\item a name at an aggregate \emph{rank} (\code{taxon_rank} such as
\code{"SPECIES AGGREGATE"}, \code{"AGGR."}, \code{"COLL. SP."}) that carries no marker
gets \code{" aggr."} appended.
}
Exported for the taxifydb build pipeline so the build and runtime sides share
one definition.
}
\keyword{internal}
