% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pick.R
\name{score_candidates}
\alias{score_candidates}
\title{Score match candidates by resolution priority}
\usage{
score_candidates(candidates)
}
\arguments{
\item{candidates}{A data.frame with \code{taxonomicStatus} and \code{taxonRank}, and
optionally \code{is_synonym} (the backbone's normalized synonym flag),
\code{fuzzy_dist} (fuzzy proximity), \code{nomenclaturalStatus} (validity),
\code{matched_name_std} and \code{accepted_name} (epithet preservation), plus
\code{authorship} and \code{accepted_authorship} (homotypy).
Two scores read the candidate's \code{n_occurrences} (records filed under its
key, a column only the GBIF backbone carries), both 0 throughout when the
column is absent and both outside the tier: which key holds the data is not
a statement about which taxon the name denotes. \code{data_score} is 0 for a key
with at least one record that keeps the name as its own concept (accepted,
doubtful or unplaced; never a synonym, whose accepted ID is another taxon)
and 1 otherwise (no records, no count, or a synonym); it orders
after \code{dist_score} and before \code{status_score}. \code{occ_score} is minus the count,
\code{Inf} where it is missing; it orders after \code{epithet_score}, so it only
separates keys the concept scores leave level.}
}
\value{
A list with the numeric vectors \code{dist_score}, \code{data_score}, \code{occ_score} and
\code{status_score}, integer vectors \code{rank_score}, \code{valid_score}, \code{epithet_score}, and the
character \code{tier} signature (\code{"dist/status/rank/epithet"}) per row, in input
order.
}
\description{
Computes the per-row priority scores used to rank backbone candidates for a
name (smaller is better): smallest fuzzy distance (\code{dist_score}), then
taxonomic status (\code{status_score}: accepted, then a synonym homotypic with its
accepted name, then a name the backbone keeps but has not reviewed, then any
other synonym, then a misapplication), SPECIES over
higher ranks (\code{rank_score}), the epithet-preserving accepted target
(\code{epithet_score}, the homotypic basionym among same-name homonym synonyms,
e.g. \verb{Pinus abies} -> \verb{Picea abies}), and finally nomenclatural validity
(\code{valid_score}). Used by the matching engine's best-match selection and, in
the \code{taxifydb} build pipeline, to collapse each backbone key to the single
accepted name \code{taxify()} resolves it to.
}
\details{
\code{dist_score} orders first because candidates for a fuzzy query are different
backbone names: the closest one is the best reading of the input, and the
remaining scores then choose among the rows carrying that name. It is 0
throughout when \code{fuzzy_dist} is absent, which is every exact-match path.

The returned \code{tier} covers the four concept scores only. \code{valid_score} orders
the pick but stays out of the tier: a nomenclaturally valid name can be a
synonym of a different species than an illegitimate one carrying the same
string, so validity must not make that conflict look resolved. Sort with
\code{\link[=candidate_order]{candidate_order()}} rather than re-listing the columns.

A synonym counts as homotypic when its own basionym author is the accepted
name's parenthetical author and the accepted name keeps its final epithet
(\verb{Lycopsis orientalis} L. -> \verb{Anchusa arvensis subsp. orientalis} (L.)
Nordh.). That outranks a record the backbone keeps unplaced under the same
string, which is a different type: a homonym, not the name the placed
record carries (#81).
}
\keyword{internal}
