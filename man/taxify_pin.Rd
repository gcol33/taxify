% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lock.R
\name{taxify_pin}
\alias{taxify_pin}
\title{Pin or release installed taxify assets}
\usage{
taxify_pin(name, pin = TRUE, kind = NULL, verbose = TRUE)
}
\arguments{
\item{name}{Character vector of installed backbone and/or enrichment names.}

\item{pin}{Logical. \code{TRUE} (default) pins; \code{FALSE} releases the pin, so the
next version check compares the build against the manifest again.}

\item{kind}{\code{NULL} (default) to find each name among the installed backbones
and enrichments, or \code{"backbone"} / \code{"enrichment"} to look only there. Needed
for a name installed as both (e.g. \code{"wcvp"}).}

\item{verbose}{Logical. Default \code{TRUE}.}
}
\value{
A data.frame with one row per name: \code{component}, \code{type}, \code{version},
\code{content_id} (the full id of the build pinned or released) and \code{pinned}.
}
\description{
A pinned build stays active: the version check that \code{\link[=taxify]{taxify()}},
\code{\link[=install_backbones]{install_backbones()}} and the \verb{add_*()} doors run against the manifest leaves
it in place instead of replacing it with the current release. Pin the builds a
project was run against to keep a shared data directory from moving under it,
and release them when the project is done. The pin is recorded in the build's
\code{meta.json} together with its content id (the md5 of its \code{.vtr}, as
\code{\link[=taxify_lock]{taxify_lock()}} records it), so a pinned build can also be named in a
lockfile.
}
\details{
Every name is checked before anything is written: when one is not installed,
or is installed as both a backbone and an enrichment and \code{kind} does not say
which, nothing is pinned.

\code{\link[=taxify_restore]{taxify_restore()}} with \code{install = TRUE} pins every build matching a lockfile,
and a build fetched with \code{taxify_download(content_id = )} or
\code{taxify_download_enrichment(content_id = )} is pinned when it is activated.
}
\examples{
\dontrun{
taxify_pin(c("wfo", "col"))
taxify_pin("iucn", kind = "enrichment")
taxify_pin(c("wfo", "col"), pin = FALSE)
}

}
\seealso{
\code{\link[=taxify_restore]{taxify_restore()}} to pin the builds a lockfile records,
\code{\link[=taxify_store]{taxify_store()}} to list the builds on disk and whether each is pinned.
}
