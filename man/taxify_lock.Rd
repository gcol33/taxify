% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lock.R
\name{taxify_lock}
\alias{taxify_lock}
\title{Record the exact backbone and enrichment versions behind a result}
\usage{
taxify_lock(x = NULL, file = NULL, verbose = TRUE)
}
\arguments{
\item{x}{A \code{\link[=taxify]{taxify()}} result (its resolved backbones and any \verb{add_*()}
enrichment layers are locked), or \code{NULL} (default) to snapshot every
installed backbone.}

\item{file}{Optional path to write the lockfile to (JSON). When \code{NULL}
(default) nothing is written and the lock is only returned.}

\item{verbose}{Logical. Default \code{TRUE}.}
}
\value{
A lock list (invisibly when \code{file} is written), with elements
\code{taxify_version}, \code{created}, \code{r_version}, \code{backends}, and \code{enrichments}.
}
\description{
Writes a machine-readable lockfile pinning which backbone and enrichment
assets -- name, version, and byte-identity (content id) -- produced a
\code{\link[=taxify]{taxify()}} result. Where \code{\link[=cite]{cite()}} prints prose citations, \code{taxify_lock()}
records the reproducible counterpart, so a manuscript's Methods can state
exactly what was matched against and \code{\link[=taxify_restore]{taxify_restore()}} can later verify an
install still matches.
}
\examples{
# Runs offline against the bundled example database.
old <- options(taxify.data_dir = taxify_example_data())

res  <- taxify("Quercus robur", backend = "wfo", verbose = FALSE)
lock <- taxify_lock(res)
lock$backends[[1]]$name

options(old)

}
\seealso{
\code{\link[=taxify_restore]{taxify_restore()}} to check an install against a lockfile, \code{\link[=cite]{cite()}}
for prose citations.
}
