% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/browse.R
\name{taxify_ids}
\alias{taxify_ids}
\title{List every accepted ID of each matched name}
\usage{
taxify_ids(x, verbose = TRUE)
}
\arguments{
\item{x}{A \code{\link[=taxify]{taxify()}} result.}

\item{verbose}{Logical. Default \code{TRUE}.}
}
\value{
A data.frame with one row per (matched input row, accepted ID), in
input order and, within a name, in the order of \code{accepted_ids} (the pick
first):
\describe{
\item{input_name}{The queried name.}
\item{backbone}{Backbone that matched the name.}
\item{accepted_id}{An accepted ID the name resolves to.}
\item{accepted_name}{The name of that accepted taxon.}
\item{authorship}{Its authorship.}
\item{rank}{Its rank.}
\item{taxonomic_status}{Its status in the backbone (\code{"ACCEPTED"}, GBIF's
\code{"DOUBTFUL"}, WFO's \code{"UNCHECKED"}, ...).}
\item{family}{Its family.}
\item{n_occurrences}{GBIF occurrence records under this ID, including
those of its synonyms and descendants (what a GBIF download by this key
returns), as counted when the backbone was built. \code{NA} on a backbone
without counts, and for an ID whose count was not taken.}
\item{is_pick}{Logical. Is this the \code{accepted_id} \code{\link[=taxify]{taxify()}} reported?}
}
Unmatched names contribute no rows.
}
\description{
\code{\link[=taxify]{taxify()}} returns one accepted ID per name, and records in \code{accepted_ids}
every accepted taxon the backbone files the name under: a homonym published
by two authors, or a name held twice, once accepted and once as a doubtful or
duplicate record. This verb lays those out one row per (name, accepted ID),
resolved against the backbone, with the ID \code{taxify()} picked marked. On the
GBIF backbone each ID carries its occurrence count, so the IDs to request
occurrence data with can be read off directly.
}
\examples{
# Runs offline against the bundled example database.
old <- options(taxify.data_dir = taxify_example_data())

taxify(c("Quercus robur", "Pinus sylvestris")) |>
  taxify_ids()

options(old)

}
\seealso{
\code{\link[=taxify]{taxify()}}, \code{\link[=synonyms]{synonyms()}}.
}
