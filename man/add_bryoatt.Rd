% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add-bryoatt.R
\name{add_bryoatt}
\alias{add_bryoatt}
\title{Add British and Irish bryophyte attributes (BRYOATT)}
\usage{
add_bryoatt(x, cols = NULL, verbose = TRUE)
}
\arguments{
\item{x}{A data.frame returned by \code{\link[=taxify]{taxify()}}.}

\item{cols}{Which columns to attach: \code{NULL} (default) the curated set,
\code{"all"} every column the source carries, or a character vector of
names. See \code{\link{enrichment_cols}}.}

\item{verbose}{Logical. Default \code{TRUE}.}
}
\value{
The same data.frame with additional columns. The default set:
\describe{
\item{bryoatt_ellenberg_light, bryoatt_ellenberg_moisture,
bryoatt_ellenberg_reaction, bryoatt_ellenberg_nitrogen,
bryoatt_ellenberg_salt}{Ellenberg indicator values calibrated for British
and Irish bryophytes.}
\item{bryoatt_life_form}{Life-form code, kept verbatim (\code{Ts}, \code{Mr},
\code{Ms}, ...).}
\item{bryoatt_plant_group}{Plant group: \code{M} moss, \code{L} liverwort,
\code{H} hornwort.}
\item{bryoatt_status}{Status code, kept verbatim (\code{N} native, \code{AN} alien
naturalised, \code{AR} archaeophyte).}
}
}
\description{
Joins attributes of British and Irish mosses, liverworts and hornworts
(Hill et al. 2007) to a \code{\link[=taxify]{taxify()}} result by looking up \code{accepted_name}.
Bryophytes are otherwise almost absent from the bundled trait databases.
}
\details{
1,194 taxa. The vascular-plant companion is \code{\link[=add_plantatt]{add_plantatt()}}.

This source states no licence (it is copyright the Biological Records
Centre), so taxify ships no pre-built copy of it. The first call builds it
from the original source on your own machine, which requires the taxifydb
package (\code{remotes::install_github("gcol33/taxifydb")}). taxify redistributes
none of the data. Cite Hill et al. (2007) when you use it.
}
\examples{
\dontrun{
# Builds the enrichment on first use (needs taxifydb).
taxify("Polytrichum commune", backbone = "gbif") |>
  add_bryoatt()
}

}
\references{
Hill MO, Preston CD, Bosanquet SDS, Roy DB (2007) BRYOATT: Attributes of
British and Irish Mosses, Liverworts and Hornworts. NERC Centre for Ecology
and Hydrology.
}
