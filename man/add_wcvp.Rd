% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add-wcvp.R
\name{add_wcvp}
\alias{add_wcvp}
\title{Add WCVP native range status}
\usage{
add_wcvp(x, region, cols = NULL, verbose = TRUE)
}
\arguments{
\item{x}{A data.frame returned by \code{\link[=taxify]{taxify()}}.}

\item{region}{Character. TDWG Level 3 region code(s), or \code{"all"}. See
\code{\link[=taxify_regions]{taxify_regions()}} for the full list of codes.
\itemize{
\item Single code (e.g., \code{"BGM"} for Belgium): adds \code{native_status}
column (no suffix).
\item Multiple codes (e.g., \code{c("BGM", "GER")}): adds
\code{native_status_BGM}, \code{native_status_GER}.
\item \code{"all"}: adds one column per region in the dataset.
}
List the region codes WCVP covers with \code{enrichment_groups("wcvp")}.}

\item{cols}{Which columns to attach. \code{NULL} (the default) attaches the
curated set; a character vector of column names attaches just those, and
\code{"all"} attaches every column the source carries (see \code{\link[=enrichment_cols]{enrichment_cols()}}).}

\item{verbose}{Logical. Default \code{TRUE}.}
}
\value{
The same data.frame with additional column(s):
\describe{
\item{native_status}{One of \code{"native"}, \code{"introduced"}, \code{"extinct"},
or \code{NA} if not recorded for that region.}
}
}
\description{
Joins WCVP (World Checklist of Vascular Plants, Kew) native range
data to a \code{\link[=taxify]{taxify()}} result, filtered by TDWG botanical region.
}
\details{
Source: WCVP (Kew, CC BY). Coverage: ~340k plant species.
Plants only.

The range reported is that of the taxon \code{\link[=taxify]{taxify()}} matched, as its
backbone circumscribes it: the matched name's own records plus those of every
name the backbone sinks into it. Where these disagree on a region, \code{native}
wins over \code{introduced}, and \code{introduced} over \code{extinct}, since the taxon is
native wherever any part of it is.

A region is left \code{NA} rather than filled from a broader taxon, or from one
the matched backbone accepts as separate, when that is the only name
another backbone offers for it. Those names are listed as \code{refused} in the
\code{wcvp} entry of \code{attr(x, "taxify_meta")$enrichments} (the queried and
refused name, the reason, the backbones offering it and the number of
regions it would have filled), and counted by \code{\link[=summary]{summary()}}.
}
\examples{
# Runs offline against the bundled example database.
old <- options(taxify.data_dir = taxify_example_data())

taxify("Quercus robur") |>
  add_wcvp(region = "GER")

taxify("Quercus robur") |>
  add_wcvp(region = c("GER", "BGM"))

options(old)

}
