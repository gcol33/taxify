% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gbif-request.R
\name{gbif_request}
\alias{gbif_request}
\title{Request GBIF occurrence data for a matched name list}
\usage{
gbif_request(
  x,
  method = c("download", "search"),
  strict = FALSE,
  limit = 500,
  format = "SIMPLE_CSV",
  dry_run = FALSE,
  ...,
  verbose = TRUE
)
}
\arguments{
\item{x}{A character vector of names, or a \code{\link[=taxify]{taxify()}} result. A character
vector is matched against the GBIF backbone first.}

\item{method}{\code{"download"} (authenticated, no cap, citable) or \code{"search"}
(unauthenticated, capped). See Choosing a method.}

\item{strict}{Logical, default \code{FALSE}. By default every accepted key each
name resolves to is requested. \code{TRUE} narrows the request to the single
key \code{\link[=taxify]{taxify()}} reported, and says how many keys, and how many records,
that leaves behind.}

\item{limit}{Records per key for \code{method = "search"}. Ignored by
\code{"download"}.}

\item{format}{Download format for \code{method = "download"}, passed to rgbif.
Ignored by \code{"search"}.}

\item{dry_run}{Logical. If \code{TRUE}, return the keys without contacting GBIF.}

\item{...}{Matching arguments passed to \code{\link[=taxify]{taxify()}} (\code{fuzzy}, \code{kingdom},
\code{region}, ...); only when \code{x} is a character vector, and must be named.}

\item{verbose}{Logical. Default \code{TRUE}.}
}
\value{
With \code{dry_run = TRUE}, an integer vector of GBIF taxon keys. With
\code{method = "download"}, the object rgbif's \code{occ_download()} returns (the
download key, to be passed to \code{occ_download_wait()}). With
\code{method = "search"}, a data.frame of occurrence records, empty if none
matched. In every case the keys and the \code{\link[=taxify_ids]{taxify_ids()}} table behind them
are attached as the \code{keys} and \code{taxa} attributes, along with the match's
\code{taxify_meta}; a download also carries its key as \code{gbif_download}, which
is what lets \code{\link[=cite]{cite()}} report the download's DOI beside the backbone.
}
\description{
Takes a list of names, matches it against the GBIF backbone and requests
occurrence data for every accepted taxon key the matches resolve to. A name
GBIF files under more than one key -- a homonym published by two authors, or
a name held once as accepted and again as a doubtful record -- contributes
all of its keys, so the request covers the whole matched concept rather than
whichever key \code{\link[=taxify]{taxify()}} reported first.
}
\details{
The keys themselves are read off \code{\link[=taxify_ids]{taxify_ids()}}, which resolves each one
against the backbone and carries its occurrence count; call it directly to
see what a request would cover before sending it, or pass \code{dry_run = TRUE}
here for the keys alone.
}
\section{Choosing a method}{

\code{method = "download"} submits an asynchronous GBIF download. It has no
record cap and yields a citable DOI, which is what GBIF asks for in
published work, and it needs a GBIF account: set \code{GBIF_USER}, \code{GBIF_PWD} and
\code{GBIF_EMAIL} in \verb{~/.Renviron}. The call returns as soon as the request is
queued; wait for it and fetch it with rgbif's \code{occ_download_wait()} and
\code{occ_download_get()}.

\code{method = "search"} sends unauthenticated searches and returns the records
directly. It needs no account, and the GBIF search API caps what it will
page through, so it suits a look at a handful of taxa rather than a
checklist-wide pull.
}

\examples{
\dontrun{
# A checklist, matched and requested in one call. Needs the full GBIF
# backbone, so it cannot run on a check machine.
spp <- c("Quercus robur", "Bellis perennis", "Morus alba")

# What would be requested, without contacting GBIF:
gbif_request(spp, dry_run = TRUE)

# An unauthenticated search:
recs <- gbif_request(spp, method = "search", limit = 50)

# A citable download (needs GBIF_USER / GBIF_PWD / GBIF_EMAIL):
dl <- gbif_request(spp, method = "download")
rgbif::occ_download_wait(dl)
rgbif::occ_download_get(dl)
}

}
\seealso{
\code{\link[=taxify_ids]{taxify_ids()}} for the keys and their occurrence counts,
\code{\link[=taxify]{taxify()}} for the matching itself.
}
