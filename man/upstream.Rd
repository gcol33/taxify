% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/upstream.R
\name{upstream}
\alias{upstream}
\title{List the higher classification (ancestors) of a taxon}
\usage{
upstream(taxon, backend = NULL, to = NULL, verbose = TRUE)
}
\arguments{
\item{taxon}{A single taxonomic name (a species, genus, or higher taxon;
synonyms and typos are resolved first).}

\item{backend}{A single backend name or a \code{taxify_backend} object. \code{NULL}
(default) uses the highest-priority installed backbone; name one that
stores the higher ranks (e.g. \code{"col"}) for a full lineage.}

\item{to}{Optional rank (or ranks) to restrict the output to -- e.g.
\code{to = "family"} answers "what family is this in?" with a single row.
\code{NULL} (default) returns the whole lineage.}

\item{verbose}{Logical. Default \code{TRUE}.}
}
\value{
A data.frame with one row per ancestor rank, columns: \code{query} (the
name as supplied), \code{accepted_name} (what it resolved to), \code{rank}, \code{name},
\code{backend}, ordered kingdom -> genus. Empty when \code{taxon} does not resolve or
the backbone stores no ranks above it.
}
\description{
Returns the lineage above \code{taxon} -- its genus, family, order, class, phylum,
and kingdom, for whichever of those ranks the backbone stores -- as a tidy
frame with one row per ancestor rank. Where \code{\link[=downstream]{downstream()}} reaches down to
descendants, \code{upstream()} reaches up to ancestors; where \code{\link[=add_classification]{add_classification()}}
attaches the ranks to an existing \code{\link[=taxify]{taxify()}} result, \code{upstream()} takes a bare
name. A synonym or misspelling is resolved to its accepted taxon first, so the
lineage returned is the accepted taxon's.
}
\examples{
# Runs offline against the bundled example database.
old <- options(taxify.data_dir = taxify_example_data())

# The lineage above a species (reptiledb carries the full higher hierarchy)
upstream("Naja naja", backend = "reptiledb")

# Just the family
upstream("Naja naja", backend = "reptiledb", to = "family")

options(old)

}
\seealso{
\code{\link[=downstream]{downstream()}} for descendants, \code{\link[=add_classification]{add_classification()}} to attach the
ranks to a \code{\link[=taxify]{taxify()}} result, \code{\link[=lowest_common]{lowest_common()}}.
}
