% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse-name.R
\name{parse_name}
\alias{parse_name}
\title{Parse taxonomic names into their structural parts}
\usage{
parse_name(x)
}
\arguments{
\item{x}{Character vector of taxonomic names.}
}
\value{
A data.frame with one row per input name and columns:
\describe{
\item{input}{The original name as supplied.}
\item{genus}{The genus (first token), or a single-letter initial for an
abbreviated genus (\code{"Q. robur"}). \code{NA} for an unresolvable hybrid
formula.}
\item{specific_epithet}{The specific epithet, or \code{NA} for a bare genus.}
\item{infrasp_rank}{The infraspecific rank marker (\code{"subsp."}, \code{"var."},
\code{"f."}, ...) when the name has one, else \code{NA}.}
\item{infrasp_epithet}{The infraspecific epithet, else \code{NA}.}
\item{authorship}{The authorship the name carries (trailing form, e.g.
\code{"(L.) H.Karst."}), or \code{NA}.}
\item{qualifier}{Any open-nomenclature / uncertainty qualifier (\code{"cf."},
\code{"aff."}, \code{"sp."}, \code{"agg."}, \code{"s.l."}, ...), canonicalized, or \code{NA}.
Infraspecific rank markers are reported in \code{infrasp_rank}, not here.}
\item{is_hybrid}{Logical. Was a hybrid marker detected?}
\item{hybrid_type}{\code{"nothogenus"}, \code{"nothospecies"}, \code{"formula"}, or \code{NA}.}
\item{rank}{\code{"genus"}, \code{"species"}, \code{"infraspecies"}, \code{"hybrid_formula"},
or \code{NA} for an empty input.}
\item{canonical}{The cleaned name used for matching (genus plus epithets,
qualifiers and authorship removed, hybrid sign dropped). \code{NA} for a
hybrid formula, which is not a single taxon.}
}
}
\description{
Decomposes each name into genus, specific epithet, infraspecific rank and
epithet, authorship, hybrid status, and any open-nomenclature qualifier,
without matching against a backbone. Where \code{\link[=taxify]{taxify()}} cleans a name and then
resolves it, \code{parse_name()} returns the cleaning step itself -- the parts of
the name -- for callers who need the decomposition rather than a match (the
role \code{rgbif::name_parse()} or gnparser fill). The same cleaning pipeline the
matcher uses drives the parse, so a name breaks apart the way it matches.
}
\examples{
parse_name(c("Quercus robur L.",
             "Poa annua var. annua",
             "Q. robur",
             "Pinus cf. sylvestris",
             "Salix alba x Salix fragilis"))

}
\seealso{
\code{\link[=taxify]{taxify()}} to match a name; \code{parse_name()} exposes the same internal
cleaning pipeline the matcher runs.
}
