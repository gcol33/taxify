% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add-euromed-distribution.R
\name{euromed_areas}
\alias{euromed_areas}
\title{Euro+Med areas and their country codes}
\usage{
euromed_areas()
}
\value{
A data.frame with \code{area_code}, \code{area_name}, \code{area_level} and \code{iso2}.
}
\description{
Lists the areas Euro+Med PlantBase states a taxon's status for, with the
ISO 3166-1 alpha-2 country code of the areas that are one country. Areas are
not countries: some combine several ("Italy, with San Marino and Vatican
City"), some are part of one (a region of European Russia, an island group),
and the level-2 areas split a combined area into its countries (\code{Au(A)}
Austria and \code{Au(L)} Liechtenstein under \code{Au}). Only an area that is exactly
one country carries an \code{iso2}.
}
\examples{
old <- options(taxify.data_dir = taxify_example_data())
euromed_areas()
options(old)
}
\seealso{
\code{\link[=add_euromed_distribution]{add_euromed_distribution()}}
}
