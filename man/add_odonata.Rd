% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add-odonata.R
\name{add_odonata}
\alias{add_odonata}
\title{Add odonate behavioural/ecological traits (OPD)}
\usage{
add_odonata(x, cols = NULL, verbose = TRUE)
}
\arguments{
\item{x}{A data.frame returned by \code{\link[=taxify]{taxify()}}.}

\item{cols}{Which columns to attach: \code{NULL} (default) the curated set, \code{"all"} every column the source carries, or a character vector of names. See \code{\link{enrichment_cols}}.}

\item{verbose}{Logical. Default \code{TRUE}.}
}
\value{
The same data.frame with categorical \code{odonata_territoriality},
\code{odonata_flight_mode}, \code{odonata_mate_guarding}, \code{odonata_habitat_openness},
\code{odonata_has_wing_pigment}.
}
\description{
Joins Odonate Phenotypic Database categorical traits to a \code{\link[=taxify]{taxify()}} result by
\code{accepted_name} (modal value per species).
}
\details{
Source: Odonate Phenotypic Database (Waller et al., Dryad, CC-BY 4.0).
}
\examples{
\dontrun{
taxify("Calopteryx splendens", backbone = "gbif") |>
  add_odonata()
}

}
\references{
Waller JT et al. The Odonate Phenotypic Database. Dryad.
\doi{10.5061/dryad.15pm5qc}
}
