% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add-fungalroot.R
\name{add_fungalroot}
\alias{add_fungalroot}
\title{Add mycorrhizal type from FungalRoot}
\usage{
add_fungalroot(x, verbose = TRUE)
}
\arguments{
\item{x}{A data.frame returned by \code{\link[=taxify]{taxify()}}.}

\item{verbose}{Logical. Default \code{TRUE}.}
}
\value{
The same data.frame with three additional columns:
\describe{
\item{mycorrhizal_type}{Genus-level majority-consensus type, one of \code{AM}
(arbuscular), \code{EcM} (ecto), \code{ErM} (ericoid), \code{OM} (orchid), \code{NM}
(non-mycorrhizal), the dual types \code{EcM-AM} / \code{ErM-EcM} / \code{ErM-AM},
\code{Other}, or \code{uncertain}. \code{NA} if the genus is not in FungalRoot.}
\item{mycorrhizal_status}{Coarse status derived from the type:
\code{"mycorrhizal"}, \code{"non-mycorrhizal"}, or \code{"uncertain"}.}
\item{mycorrhizal_records}{Number of FungalRoot observations supporting the
genus-level consensus.}
}
}
\description{
Joins genus-level mycorrhizal type from the FungalRoot database
(Soudzilovskaia et al. 2020) to a \code{\link[=taxify]{taxify()}} result by looking up \code{genus}.
Mycorrhizal type is phylogenetically conserved at the genus level, which is
the resolution FungalRoot recommends for inference, so this enrichment joins
on \code{genus} rather than \code{accepted_name}.
}
\details{
Source: FungalRoot, published on GBIF as a Darwin Core Archive
(\doi{10.15468/a7ujmj}), CC BY-NC 4.0. The per-genus value is a majority
consensus computed from the per-observation mycorrhiza type labels, not
FungalRoot's own published per-genus assignment. Plant genera only. The
\code{.vtr} is downloaded from the taxify release on first use and cached.
}
\examples{
\donttest{
# Joins on genus, so any species in a covered genus is annotated.
taxify(c("Quercus robur", "Trifolium pratense")) |>
  add_fungalroot()
}

}
\references{
Soudzilovskaia NA et al. (2020) FungalRoot: global online database of plant
mycorrhizal associations. New Phytologist 227:955-966.
}
