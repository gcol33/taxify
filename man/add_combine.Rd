% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add-combine.R
\name{add_combine}
\alias{add_combine}
\title{Add mammal traits (COMBINE)}
\usage{
add_combine(x, cols = NULL, verbose = TRUE)
}
\arguments{
\item{x}{A data.frame returned by \code{\link[=taxify]{taxify()}}.}

\item{cols}{Which columns to attach: \code{NULL} (default) the curated set,
\code{"all"} every column the source carries, or a character vector of
names. See \code{\link{enrichment_cols}}.}

\item{verbose}{Logical. Default \code{TRUE}.}
}
\value{
The same as \code{\link[=add_combine_reported]{add_combine_reported()}} (the \verb{combine_*} reported
columns).
}
\description{
A thin wrapper around \code{\link[=add_combine_reported]{add_combine_reported()}}, kept so existing code keeps
working. New code should call \code{\link[=add_combine_reported]{add_combine_reported()}} (reported values) or
\code{\link[=add_combine_imputed]{add_combine_imputed()}} (phylogenetically imputed values) explicitly.
}
\examples{
\donttest{
taxify("Vulpes vulpes", backend = "gbif") |>
  add_combine()
}
}
\seealso{
\code{\link[=add_combine_reported]{add_combine_reported()}}, \code{\link[=add_combine_imputed]{add_combine_imputed()}}
}
