% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add-iucn.R
\name{add_iucn}
\alias{add_iucn}
\title{Add IUCN Red List conservation status}
\usage{
add_iucn(x, verbose = TRUE)
}
\arguments{
\item{x}{A data.frame returned by \code{\link[=taxify]{taxify()}}.}

\item{verbose}{Logical. Show download progress if enrichment data needs
to be fetched. Default \code{TRUE}.}
}
\value{
The same data.frame with an additional column:
\describe{
\item{conservation_status}{IUCN category: \code{"LC"} (Least Concern),
\code{"NT"} (Near Threatened), \code{"VU"} (Vulnerable), \code{"EN"} (Endangered),
\code{"CR"} (Critically Endangered), \code{"EW"} (Extinct in the Wild),
\code{"EX"} (Extinct), or \code{NA} if not assessed.}
}
}
\description{
Joins IUCN Red List conservation status to a \code{\link[=taxify]{taxify()}} result by looking up
\code{accepted_name}. This is the source-named door for the IUCN Red List; for
conservation status reconciled across sources, use \code{\link[=add_trait]{add_trait()}} once more
than one source is registered for it.
}
\details{
Conservation status values are compiled from publicly available sources
including GBIF and the IUCN Red List API. Coverage is global across all
taxonomic groups (~166k species).
}
\examples{
# Runs offline against the bundled example database.
old <- options(taxify.data_dir = taxify_example_data())

taxify("Panthera tigris", backend = "gbif") |>
  add_iucn()

options(old)

}
