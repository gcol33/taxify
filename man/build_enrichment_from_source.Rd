% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enrichment-build.R
\name{build_enrichment_from_source}
\alias{build_enrichment_from_source}
\title{Build an enrichment .vtr from original source data}
\usage{
build_enrichment_from_source(name, url = NULL, verbose = TRUE)
}
\arguments{
\item{name}{Character. Enrichment identifier (e.g., \code{"conservation_status"},
\code{"griis"}, \code{"alien_first_records"}). See \code{list_enrichments()} for
available names.}

\item{url}{Character or \code{NULL}. Custom source URL to download from instead
of the default. The same download and parse pipeline is used, so the file
at the URL must have the same format as the original source. Default
\code{NULL} (use the registry's built-in URL).}

\item{verbose}{Logical. Print progress messages. Default \code{TRUE}.}
}
\value{
Path to the built \code{.vtr} file (invisibly).
}
\description{
Downloads the original source data, parses it using the enrichment's
built-in pipeline (cleaning, deduplication, country mapping, etc.), and
writes a local \code{.vtr} file. Use this to rebuild an enrichment with the
latest upstream data, or point it at a custom URL for a newer release.
}
\examples{
\dontrun{
# Rebuild from default source
build_enrichment_from_source("conservation_status")

# Rebuild from a newer release URL
build_enrichment_from_source(
  "alien_first_records",
  url = "https://figshare.com/ndownloader/articles/6192923/versions/4"
)
}

}
