% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gbif-request.R
\name{gbif_backmatch}
\alias{gbif_backmatch}
\title{Link GBIF occurrence records back to the names they were requested for}
\usage{
gbif_backmatch(records, x, verbose = TRUE)
}
\arguments{
\item{records}{A data.frame of occurrence records: what
\code{gbif_request(method = "search")} returns, or a download imported with
rgbif's \code{occ_download_import()}.}

\item{x}{What the keys came from: the object \code{\link[=gbif_request]{gbif_request()}} returned, a
\code{\link[=taxify_ids]{taxify_ids()}} table, or a \code{\link[=taxify]{taxify()}} result.}

\item{verbose}{Logical. Default \code{TRUE}.}
}
\value{
\code{records} with three columns added: \code{requested_key} (the key that
matched), \code{input_name} (the name as queried) and \code{accepted_name}. Records
matching no requested key keep \code{NA} in all three.
}
\description{
A request built from \code{\link[=gbif_request]{gbif_request()}} covers several keys per name, so the
records that come back have to be traced to the name that asked for them
before they can be counted or grouped.
}
\details{
Joining on \code{taxonKey} alone loses records, silently. GBIF returns the
records of a key's descendants as well as its own, and a record identified
to a subspecies carries the subspecies key: requesting \emph{Pinus nigra}
(5284809) returns records whose \code{taxonKey} is 5686674, \emph{P. nigra} subsp.
\emph{salzmannii}. Those rows match on \code{speciesKey} and on nothing else. This
function therefore tries the record's keys from the most specific outwards
and takes the first that is one of the requested keys.
}
\examples{
\dontrun{
spp <- c("Pinus nigra", "Quercus robur")
recs <- gbif_request(spp, method = "search", limit = 100)

recs <- gbif_backmatch(recs, recs)
table(recs$input_name, useNA = "ifany")
}

}
\seealso{
\code{\link[=gbif_request]{gbif_request()}}, \code{\link[=taxify_ids]{taxify_ids()}}.
}
