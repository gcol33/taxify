% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gbif-request.R
\name{gbif_fetch}
\alias{gbif_fetch}
\title{Fetch the records of a submitted GBIF download}
\usage{
gbif_fetch(x, backmatch = TRUE, path = tempdir(), verbose = TRUE)
}
\arguments{
\item{x}{What \code{gbif_request(method = "download")} returned.}

\item{backmatch}{Logical. Attach the queried names with \code{\link[=gbif_backmatch]{gbif_backmatch()}}.
Default \code{TRUE}.}

\item{path}{Directory to download into. Defaults to a session temp
directory, so nothing is written outside it unless you ask.}

\item{verbose}{Logical. Default \code{TRUE}.}
}
\value{
A data.frame of occurrence records, carrying the same \code{keys},
\code{taxa}, \code{taxify_meta} and \code{gbif_download} attributes as \code{x}, so \code{\link[=cite]{cite()}}
reports the backbone and every download DOI.
}
\description{
Waits for the download \code{\link[=gbif_request]{gbif_request()}} submitted, retrieves it, imports it
and returns the records. A request too long for one GBIF query is split
across several downloads, and all of them are waited for and stacked, so a
sharded request is fetched the same way as a single one.
}
\examples{
\dontrun{
dl <- gbif_request(c("Quercus robur", "Bellis perennis"))
recs <- gbif_fetch(dl)
cite(recs)
}

}
\seealso{
\code{\link[=gbif_request]{gbif_request()}}, \code{\link[=gbif_backmatch]{gbif_backmatch()}}, \code{\link[=cite]{cite()}}.
}
