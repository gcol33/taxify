% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/basionym.R
\name{basionym_placement}
\alias{basionym_placement}
\title{Find the placed basionym of each unplaced backbone record}
\usage{
basionym_placement(records, basionyms)
}
\arguments{
\item{records}{Backbone rows (unified schema) with \code{taxon_id},
\code{taxonomic_status}, \code{original_name_usage_id} and optionally \code{is_synonym}.}

\item{basionyms}{Backbone rows to find the basionyms among, with \code{taxon_id},
\code{taxonomic_status}, \code{accepted_taxon_id} and optionally \code{is_synonym}.}
}
\value{
Integer vector along \code{records}: the row of \code{basionyms} whose accepted
taxon the record belongs to, or \code{NA}.
}
\description{
The one definition of when a basionym places a record, shared by the
matching engine and taxifydb's name-lookup build. A record qualifies when the
backbone keeps it unplaced (the status grade of \code{.status_unplaced}) and its
\code{original_name_usage_id} names another record that the backbone places:
accepted, or a plain synonym, with an accepted taxon other than the record
itself. A basionym that is itself unplaced, misapplied or absent settles
nothing.
}
\keyword{internal}
