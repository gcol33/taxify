% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add-zanne.R
\name{add_zanne}
\alias{add_zanne}
\title{Add woodiness (Zanne et al. 2014)}
\usage{
add_zanne(x, verbose = TRUE)
}
\arguments{
\item{x}{A data.frame returned by \code{\link[=taxify]{taxify()}}.}

\item{verbose}{Logical. Default \code{TRUE}.}
}
\value{
The same data.frame with an additional column:
\describe{
\item{woodiness}{One of \code{"woody"}, \code{"herbaceous"}, \code{"variable"},
or \code{NA} if not in the dataset.}
}
}
\description{
Joins the woody / herbaceous classification of Zanne et al. (2014) to a
\code{\link[=taxify]{taxify()}} result by looking up \code{accepted_name}. This is the source-named
door for the Zanne Global Woodiness Database; for woodiness reconciled across
every source that carries it (Zanne, GIFT), use \code{\link[=add_trait]{add_trait()}} with
\code{"woodiness"}.
}
\details{
Source: Zanne et al. 2014, Nature (Dryad, CC0). Coverage: ~50k plant
species. Plants only.
}
\examples{
# Runs offline against the bundled example database.
old <- options(taxify.data_dir = taxify_example_data())

taxify("Quercus robur") |>
  add_zanne()

options(old)

}
\references{
Zanne AE et al. (2014) Three keys to the radiation of angiosperms into
freezing environments. Nature 506:89-92.
}
\seealso{
\code{\link[=add_trait]{add_trait()}} for woodiness harmonized across sources.
}
