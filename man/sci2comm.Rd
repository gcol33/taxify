% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/comm2sci.R
\name{sci2comm}
\alias{sci2comm}
\title{Resolve scientific names to common (vernacular) names}
\usage{
sci2comm(x, lang = NULL, resolve = TRUE, backend = NULL, verbose = TRUE)
}
\arguments{
\item{x}{Character vector of scientific names.}

\item{lang}{Character. Restrict to one language: an ISO 639-1 code (\code{"en"},
\code{"de"}, ...) as used by the GBIF source, or \code{NA} for the untagged
NCBI/Open Tree names. \code{NULL} (default) returns every language. List the
languages present with \code{enrichment_groups("common_names")}.}

\item{resolve}{Logical. When \code{TRUE} (default), each input is run through
\code{\link[=taxify]{taxify()}} first so a synonym or misspelling reports its accepted taxon's
vernaculars. When \code{FALSE}, the input name is looked up verbatim (faster,
offline; use when the names are already accepted).}

\item{backend}{Passed to \code{\link[=taxify]{taxify()}} when \code{resolve = TRUE}; \code{NULL} (default)
uses every installed backbone. Ignored when \code{resolve = FALSE}.}

\item{verbose}{Logical. Default \code{TRUE}.}
}
\value{
A data.frame with one row per (query, vernacular):
\describe{
\item{query}{The scientific name as supplied.}
\item{scientific_name}{The accepted name looked up (equals \code{query} when
\code{resolve = FALSE} or the query was already accepted).}
\item{common_name}{A vernacular name.}
\item{lang}{Language tag (\code{NA} for NCBI/Open Tree).}
}
A query with no vernacular contributes no rows.
}
\description{
The forward direction of \code{\link[=comm2sci]{comm2sci()}}: given a scientific name, return the
common name(s) it is known by. Reads the bundled \code{common_names} enrichment
(GBIF, NCBI, and Open Tree vernaculars) offline; the first call may trigger
the one-time download. Where \code{\link[=add_common_names]{add_common_names()}} attaches vernaculars to a
\code{\link[=taxify]{taxify()}} result as columns, \code{sci2comm()} takes bare names and returns the
long lookup table -- one row per (name, vernacular). A name maps to many
common names (across languages and synonyms), so a query can carry several
rows.
}
\examples{
# Runs offline against the bundled example database.
old <- options(taxify.data_dir = taxify_example_data())

# The bundled example maps Quercus robur -> "example_common_name" (en + de)
sci2comm("Quercus robur", resolve = FALSE)

options(old)

}
\seealso{
\code{\link[=comm2sci]{comm2sci()}} for the reverse direction (common -> scientific),
\code{\link[=add_common_names]{add_common_names()}} to attach vernaculars to a \code{\link[=taxify]{taxify()}} result.
}
