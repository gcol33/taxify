% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backbones.R
\name{backbone_fixed_kingdom}
\alias{backbone_fixed_kingdom}
\title{The single kingdom a backbone's rows always belong to, or NA}
\usage{
backbone_fixed_kingdom(name)
}
\arguments{
\item{name}{Character vector of backbone names.}
}
\value{
Character vector of coarse kingdom groups (see
\code{\link[=normalize_kingdom_group]{normalize_kingdom_group()}}), \code{NA} where the backbone spans more than one
kingdom or is not a known backbone.
}
\description{
A backbone scoped to one kingdom by construction (the vascular-plant
backbones, Species Fungorum) carries no \code{kingdom} column, since it has
nothing to record there. This names that kingdom, so a row from such a
backbone can still be placed.
}
\examples{
backbone_fixed_kingdom(c("wfo", "fungorum", "gbif"))
}
\keyword{internal}
