% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add-glonaf.R
\name{add_glonaf}
\alias{add_glonaf}
\title{Add naturalized alien flora status (GloNAF)}
\usage{
add_glonaf(x, region, verbose = TRUE)
}
\arguments{
\item{x}{A data.frame returned by \code{\link[=taxify]{taxify()}}.}

\item{region}{Character. GloNAF region identifier(s), or \code{"all"}.
Regions use TDWG-compatible codes extended with dot notation for
sub-national units (e.g., \code{"USA.CA"} for California).
\itemize{
\item Single region: adds \code{naturalized} column (no suffix).
\item Multiple regions: adds \verb{naturalized_<region>} columns.
\item \code{"all"}: adds one column per region in the dataset.
}}

\item{verbose}{Logical. Default \code{TRUE}.}
}
\value{
The same data.frame with additional column(s):
\describe{
\item{naturalized}{Integer \code{1} if the species is recorded as
naturalized in that region, \code{NA} otherwise.}
}
}
\description{
Joins GloNAF (Global Naturalized Alien Flora) data to a \code{\link[=taxify]{taxify()}}
result, filtered by region.
}
\details{
Source: GloNAF v2.0 (van Kleunen et al. 2019, Davis et al. 2025,
CC BY 4.0). Coverage: ~16k alien plant taxa across ~1,300 regions.
Plants only.
}
\examples{
# Runs offline against the bundled example database.
old <- options(taxify.data_dir = taxify_example_data())

taxify("Robinia pseudoacacia") |>
  add_glonaf(region = "EUR")

taxify("Robinia pseudoacacia") |>
  add_glonaf(region = c("EUR", "NAM"))

options(old)

}
\references{
van Kleunen M et al. (2019) The Global Naturalized Alien Flora
(GloNAF) database. Ecology 100:e02542.

Davis K et al. (2025) The updated Global Naturalized Alien Flora
(GloNAF 2.0) database. Ecology, e70245.
}
