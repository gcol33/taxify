% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/browse.R
\name{add_classification}
\alias{add_classification}
\title{Add the full higher classification to a taxify result}
\usage{
add_classification(
  x,
  ranks = c("kingdom", "phylum", "class", "order"),
  verbose = TRUE
)
}
\arguments{
\item{x}{A data.frame returned by \code{\link[=taxify]{taxify()}}.}

\item{ranks}{Character vector of ranks to attach. Default
\code{c("kingdom", "phylum", "class", "order")}.}

\item{verbose}{Logical. Default \code{TRUE}.}
}
\value{
\code{x} with the requested rank columns added. A rank a backbone does not
store is left \code{NA} (WFO, for example, carries no ranks above family).
}
\description{
Attaches the Linnaean ranks above family (kingdom, phylum, class, order) to a
\code{\link[=taxify]{taxify()}} result by joining each matched row back to its backbone. The core
\code{taxify()} output already carries \code{family} and \code{genus}; this fills the ranks
above them, for whichever ranks the matched backbone stores. Rows matched by
different backends are each joined against their own backbone.
}
\examples{
# Runs offline against the bundled example database.
old <- options(taxify.data_dir = taxify_example_data())

taxify("Naja naja", backend = "reptiledb") |>
  add_classification()

options(old)

}
\seealso{
\code{\link[=add_col_info]{add_col_info()}}, \code{\link[=add_gbif_info]{add_gbif_info()}} for backend-specific extras.
}
