% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add-eberswalde.R
\name{add_eberswalde}
\alias{add_eberswalde}
\title{Add Eberswalde long-term carabid monitoring traits and trends}
\usage{
add_eberswalde(x, cols = NULL, verbose = TRUE)
}
\arguments{
\item{x}{A data.frame returned by \code{\link[=taxify]{taxify()}}.}

\item{cols}{Which columns to attach: \code{NULL} (default) all of them, or a
character vector of names. See \code{\link{enrichment_cols}}.}

\item{verbose}{Logical. Default \code{TRUE}.}
}
\value{
The same data.frame with \code{eberswalde_} columns: numeric
\code{body_length_mm}, \code{humidity_pref}, \code{range_centre_lat}, \code{abundance_total};
categorical \code{wing_morph}, \code{feeding_guild}, \code{abundance_trend},
\code{drought_effect}.
}
\description{
Joins ground-beetle traits and 24-year local population trends to a
\code{\link[=taxify]{taxify()}} result by accepted name.
}
\details{
Source: Weiss, von Wehrden & Linde (2024), 27 species pitfall-trapped
on 13 forest plots near Eberswalde between 1999 and 2022.

\code{body_length_mm}, \code{wing_morph} and \code{range_centre_lat} are carabids.org
(Homburg et al. 2014) verbatim, which the deposit's README states and the
data confirm: over the 19 species shared with \code{\link[=add_chowdhury]{add_chowdhury()}} every size
is exactly equal and every wing class agrees. They are surfaced here
because a door should show what its source carries, and they are kept out
of \code{\link[=add_trait]{add_trait()}}, where they would double-count one lineage.

What belongs to this deposit alone is the monitoring result:
\code{abundance_trend} over the 24 years, \code{drought_effect} against the 72-month
SPEI index, and a \code{feeding_guild} refined by the authors' field
observations to name the prey. \code{humidity_pref} is Sustek's (2004) 1-8
scale, 1 dry to 8 humid.
}
\examples{
\dontrun{
taxify(c("Carabus coriaceus", "Nebria brevicollis")) |>
  add_eberswalde()
}

}
\references{
Weiss F, von Wehrden H, Linde A (2024) Eberswalde Carabid Monitoring
1999-2022 - Full Data. PubData Leuphana. \doi{10.48548/pubdata-46}

Weiss F, von Wehrden H, Linde A (2024) Long-term drought triggers severe
declines in carabid beetles in a temperate forest. Ecography 2024(4):e07020.
\doi{10.1111/ecog.07020}
}
