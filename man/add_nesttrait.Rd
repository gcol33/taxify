% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add-nesttrait.R
\name{add_nesttrait}
\alias{add_nesttrait}
\title{Add bird nest traits (NestTrait)}
\usage{
add_nesttrait(x, cols = NULL, verbose = TRUE)
}
\arguments{
\item{x}{A data.frame returned by \code{\link[=taxify]{taxify()}}.}

\item{cols}{Which columns to attach: \code{NULL} (default) the curated set, \code{"all"} every column the source carries, or a character vector of names. See \code{\link{enrichment_cols}}.}

\item{verbose}{Logical. Default \code{TRUE}.}
}
\value{
The same data.frame with three collapsed categorical columns
(\code{nesttrait_structure}, \code{nesttrait_site}, \code{nesttrait_attachment}, each a
pipe-delimited set of modalities for multi-modal species) plus 20 raw 0/1
indicator columns prefixed \code{nesttrait_}: \code{brood_parasite}, \code{mound_builder},
seven \verb{nestsite_*}, seven \verb{neststr_*} and four \verb{nestatt_*} flags.
}
\description{
Joins bird nest-site, nest-structure and nest-attachment indicators to a
\code{\link[=taxify]{taxify()}} result by \code{accepted_name}. Each trait is a 0/1 presence flag; a
species may carry several flags within a group.
}
\details{
Source: NestTrait v2 (Chia et al. 2023, Scientific Data, CC-BY 4.0).
}
\examples{
\dontrun{
taxify("Turdus merula", backend = "gbif") |>
  add_nesttrait()
}

}
\references{
Chia SY et al. (2023) A global database of bird nest traits. Scientific Data.
\doi{10.1038/s41597-023-02837-1}
}
