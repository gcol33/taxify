% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classification-tree.R
\name{class2tree}
\alias{class2tree}
\title{Build a taxonomy tree from resolved names}
\usage{
class2tree(x, backend = NULL, verbose = TRUE)
}
\arguments{
\item{x}{Character vector of names, or a \code{\link[=taxify]{taxify()}} result.}

\item{backend}{Backend passed to \code{\link[=taxify]{taxify()}} when \code{x} is raw names. \code{NULL}
(default) uses every installed backbone. Ignored when \code{x} is a result.}

\item{verbose}{Logical. Default \code{TRUE}.}
}
\value{
An object of class \code{taxify_tree}: a list with
\describe{
\item{newick}{The Newick string (internal nodes labelled by rank value,
tips by species name).}
\item{classification}{The classification data.frame the tree was built
from.}
\item{tip_labels}{The species at the tips.}
\item{phylo}{An \pkg{ape} \code{phylo} object, or \code{NULL} if \pkg{ape} is not
installed / the string could not be parsed.}
}
}
\description{
Resolves the names, attaches their full higher classification, and assembles a
taxonomy tree (kingdom -> phylum -> class -> order -> family -> genus ->
species) from the shared lineages. Returns the tree as a Newick string and the
underlying classification table; when the \pkg{ape} package is installed, an
\code{ape} \code{phylo} object is included too.
}
\examples{
# Runs offline against the bundled example database.
old <- options(taxify.data_dir = taxify_example_data())

tr <- class2tree(c("Quercus robur", "Quercus petraea", "Quercus pyrenaica"))
tr$newick

options(old)

}
\seealso{
\code{\link[=lowest_common]{lowest_common()}}, \code{\link[=add_classification]{add_classification()}}, \code{\link[=taxify]{taxify()}}.
}
