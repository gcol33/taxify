% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pick.R
\name{candidate_order}
\alias{candidate_order}
\title{Order match candidates by resolution priority}
\usage{
candidate_order(candidates, scores = NULL, group_col = NULL)
}
\arguments{
\item{candidates}{A data.frame accepted by \code{\link[=score_candidates]{score_candidates()}}, carrying a
\code{taxonID} column and, when \code{group_col} is given, that column too.}

\item{scores}{The \code{\link[=score_candidates]{score_candidates()}} output for \code{candidates}, when it has
already been computed; recomputed when \code{NULL}.}

\item{group_col}{Character or \code{NULL}. Column to sort by ahead of the scores.}
}
\value{
An integer permutation of \code{seq_len(nrow(candidates))}.
}
\description{
The single source of truth for the candidate sort: the four concept scores of
\code{\link[=score_candidates]{score_candidates()}} in tier order, then the nomenclatural-validity tiebreak,
then the lowest \code{taxonID}. Pass \code{group_col} to sort within groups first, so
the first row of each group is that group's best candidate.
}
\keyword{internal}
