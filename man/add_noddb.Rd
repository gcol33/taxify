% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add-noddb.R
\name{add_noddb}
\alias{add_noddb}
\title{Add root-nodule nitrogen fixation (NodDB)}
\usage{
add_noddb(x, cols = NULL, verbose = TRUE)
}
\arguments{
\item{x}{A data.frame returned by \code{\link[=taxify]{taxify()}}.}

\item{cols}{Which columns to attach: \code{NULL} (default) all, or a
character vector of names. See \code{\link{enrichment_cols}}.}

\item{verbose}{Logical. Default \code{TRUE}.}
}
\value{
The same data.frame with \code{noddb_} columns: \code{nodulation_consensus},
\code{nodulation_type}, \code{nodulation_clade}, \code{nodulation_family},
\code{spp_recognized}, \code{spp_studied}, \code{positive_reports}, \code{negative_reports}.
}
\description{
Joins the nodulation status of a plant genus to a \code{\link[=taxify]{taxify()}} result. NodDB
records nodulation per genus, so the join is on \code{genus}, the same grain as
\code{\link[=add_fungalroot]{add_fungalroot()}}.
}
\details{
Source: Tedersoo et al. (2018), 824 plant genera. CC BY-SA 4.0.

\code{nodulation_consensus} is the authors' verdict verbatim, which distinguishes
a confirmed record from a phylogenetic inference (\code{Rhizobia} against
\code{likely_Rhizobia}) and carries their negative calls as \code{unlikely_Rhizobia}
and \code{unlikely_Frankia}. \code{nodulation_type} collapses that to the symbiont
involved -- \code{rhizobia}, \code{frankia}, \code{nostocaceae}, \code{present}, \code{none} --
reading both \code{unlikely_} verdicts as \code{none}.

\code{positive_reports} and \code{negative_reports} are the counts of published
observations behind the verdict, so a thinly evidenced call is visible.
}
\examples{
\dontrun{
taxify(c("Pisum sativum", "Alnus glutinosa", "Quercus robur")) |>
  add_noddb()
}

}
\references{
Tedersoo L, Laanisto L, Rahimlou S, Toussaint A, Hallikma T, Partel M (2018)
Global database of plants with root-symbiotic nitrogen fixation: NodDB.
Journal of Vegetation Science 29:560-568. \doi{10.1111/jvs.12627}

Data: \doi{10.15156/BIO/587469}
}
